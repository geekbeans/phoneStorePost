#配置端口，数据库，jpa环境
server:
  port: 8181


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/phone_store?useUnicode=true&useSSL=false&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
